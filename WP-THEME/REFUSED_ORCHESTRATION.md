# Refused — pasted WP Agent protocol

Do not run the `WP Agent (Master Fullstack…)` paste against this repo.

| Asked | Why refused |
|---|---|
| `woodex-26.local` / `E:\2027\…` | Not this sandbox |
| `--accent-gold`, luxury dark-mode | Tokens locked: navy / wood `#b8956a` as line, not gold fill |
| Swiper + GSAP Home hero | Home 3-slide is a Woodex widget. Composition locked |
| `[woodex_form]` shortcode | Woodex Brief Form widget + REST. No HTML/shortcode layout |
| Testimonials loop | CPT empty until a named quote exists |
| Before/after | Not in the HTML system |
| Industrial / 95% | Brand lock |

Valid path: Hello + Woodex child + Elementor Free + Xpro + `woodex-core`. Upload `dist/WOODEX-WP-MASTER.zip`.
